<% @page_title = "تحديث البيانات التعليمية" %>
<div id="content-header">
  <div class='header-icon student-icon'></div>
  <h1>تسجيل</h1>
  <div class='header-sep'>|</div>
  <div class='sub-header'>الخطوة 4 - تفاصيل الطالب</div>
</div>
<div id="page-yield">
  <%= form_for(@student) do |f| %>
  <%= render(:partial =>'education_details_form', :locals => {:f => f }) %>
  <%= hidden_field_tag('first_time', false ) %>
  <%= hidden_field_tag('updated_part', 'fourth_step' ) %>
  <div><%= f.submit("تحديث", :class => 'submit_button') %></div>
  <% end %>
</div>
</div>
